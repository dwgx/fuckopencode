node_modules/
dist/
coverage/
*.log
.env
.env.*
!.env.example
.DS_Store

# 线上 dist 备份（部署时生成，不入库）
.deploy-backup/

# 本地预览配置（含测试用 key，不入库）
.claude/launch.json

# 用量库（运行时产物；线上落在 dist/ 外面，deploy 时不会被覆盖）
data/

# 面板本地截图（会话调试产物，不入库）
.opencode/tmp/
